# Go
/bin/
/dist/
*.test
*.out
vendor/
go.work
go.work.sum

# Nix
result
result-*
.direnv/
.envrc.local

# editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# controller-gen output (regenerated, do not commit)
/config/crd/bases/
/config/rbac/role.yaml

# Helm chart templates are generated by nix/dionysus/deployment.nix and
# committed to git so Flux can render them without running nix. Regenerate
# via `nix build .#helm-chart` and copy from the result if deployment.nix
# changes.
/config/
